1 person used for git commands>>

Team >>

multiple persons working on same branch >>


======================================================
git -Version Control System
git hub (open source),GitLab, azure DevOps , bit Bicket --cloud  platforms


git --version (git download)

account creation >> Repo Creation >> Public / Private >> copy git clone command (url) -- username , Email ID

git config  / git config --global >> -- username , Email ID

git clone url --- main /master

added a new file demo1.txt, demo2.txt,demo3.txt,demo4.txt

git status

git add demo1.txt ,demo4.txt / git add . (new all files)

git commit -m "adding demo file"

git push
============================================
main
git branch

git checkout -b <branch-name>  -------new branch from local
git checkout -b demo-branch
main >>demo-branch

git checkout main >> branch switching
demo-branch >> main
===================================
1 person

git clone

git checkout -b <branch-name>  -------new branch from local

git checkout main >> branch switching

git status

git add demo1.txt ,demo4.txt / git add . (new all files)

git commit -m "adding demo file"

git push
====================================
git checkout -b <branch-name>  -------new branch from local

git checkout main >> branch switching

Team 4 >> b1 ,b2, b3, b4  >> main

git branch

git checkout -b b4

git push (b4) >> local to remote

Pull Request  >> remote b4 to remote main >> PR create >>Merge (main)


local >> b4 - checkout to main  >> git pull origin main
====================================
2 persons working on same branch

b4 --- login  , add to cart

git branch -b4

code push Username=vijay1, pwd=123

code devlop ---Username=vijay2, pwd=12345
git  add .

git commit

git pull (later chnages from remote to local )

git push


working directory 
git clone
git status
git checkout -b branchname
git status or git branch 
git add filenames or add. 
git status 
git commit -m "description"
git push (changes from local repo to remote repo branch ) if remote repo branch does not exist then we have to use --setup --up stream origin branchnameoflocalrepo so that i will create the remote repo branch and then push our local repo branch code to remote repo branch
if we have to sync the latest changes to our branch then we have to use 
git pull origin main (here main is the branch name if we have diff branch then use that branch name)



we can see the files added locally in any branch but after adding and commit and push our files from local repo to remote repo and then merge it wont be available in other branches

so all files are linked to that particualr  branches once those are pushed to remote repo until then it will be shown in all branches 



main branch - latest code from remote to local repo 

defect fix 1- branch 1 created on top of main branch - file 1,file 2, file 3 along with latest repo of main branch - add commit and then only check out 

dev fix - branch 2- file 4 , file 5,file 6 (file 1,file 2, file 3)

